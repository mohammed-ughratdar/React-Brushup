export type Holiday = {
    id: string
    startDate: string
    name: [
        {
            text: string
        }
    ]
}

export type Country = {
    isoCode: string
    name: [
        {
            text: string
        }
    ]
}